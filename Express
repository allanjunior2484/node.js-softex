const express = require('express');
const app = express();

// rota para requisições GET
app.get('/', (req, res) => {
  res.send('Esta é uma requisição GET.');
});

// rota para requisições POST
app.post('/', (req, res) => {
  res.send('Esta é uma requisição POST.');
});

// rota para requisições PUT
app.put('/', (req, res) => {
  res.send('Esta é uma requisição PUT.');
});

// rota para requisições DELETE
app.delete('/', (req, res) => {
  res.send('Esta é uma requisição DELETE.');
});

// Iniciar o servidor
app.listen(3000, () => {
  console.log('Servidor iniciado na porta 3000');
});




Salve o arquivo com o nome "index.js".
Abra o terminal e navegue até o diretório onde o arquivo "index.js" está localizado.
Execute o comando npm install express para instalar a biblioteca Express.
Execute o comando node index.js para iniciar o servidor.
Abra um navegador e acesse a URL "http://localhost:3000/" para testar as requisições GET, POST, PUT e DELETE.
